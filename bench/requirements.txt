# Bench-only dependencies. Kept deliberately tiny so a fresh git
# clone can bootstrap in seconds.
#
# The bench TALKS TO Ollama via HTTP — it does not run inference
# itself. That's why the heavy ML stack (torch, sentence-transformers,
# fastembed, etc.) is NOT here.
httpx>=0.27
psutil>=5.9
py-cpuinfo>=9.0
